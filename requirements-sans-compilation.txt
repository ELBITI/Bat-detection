# Dépendances sans compilation (versions flexibles = wheels précompilés)
# À utiliser si "pip install -r requirements.txt" échoue (contourpy, etc.)
# Évite d'avoir besoin de Visual Studio Build Tools sur Windows

streamlit>=1.26
ultralytics>=8.0
opencv-python-headless>=4.8
easyocr>=1.7
Pillow>=9.5
pytube>=15.0
numpy>=1.24
torch>=2.0
torchvision>=0.15
PyYAML>=6.0
omegaconf>=2.3
imageio>=2.31
